doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title Document
  link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css')
  link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css')
  link(rel='stylesheet' href='/css/saleslist.css')
body 
  nav.navbar.navbar-expand-lg.bg-body-tertiary
      .container-fluid
        a.navbar-brand(href=disabled) Sales List
        button.navbar-toggler(type='button' data-bs-toggle='collapse' data-bs-target='#navbarNav' aria-controls='navbarNav' aria-expanded='false' aria-label='Toggle navigation')
          span.navbar-toggler-icon
        #navbarNav.collapse.navbar-collapse
          ul.navbar-nav
            li.nav-item
              a.nav-link.active(aria-current='page' href='/') Home
            li.nav-item
              a.nav-link(href='/products/producelist') View Produce
            li.nav-item
              a.nav-link(href='/') Log out
           
.container
    .table-responsive
        table
            thead
              tr
                th Customer Name
                th Produce Name
                th Quantity
                th Amount(shs)
                th Sales Agent
                th Date
                th Update
                th Delete
            tbody
              if sales.length
                each sale in sales
                  tr
                    td #{sale.buyerName}
                    td #{sale.product}
                    td #{sale.quantity}
                    td #{sale.amount}
                    td #{sale.agentName}
                    td #{moment(sale.date).format("DD-MM-YYYY")}
                    td
                        a.btn.btn-sm.btn-outline-primary(href="/sales/updateSale/"+sale._id)
                            i.bi.bi-pencil-square.me-1
                            | Update
                    td 
                        form(d="inline" action="/sales/deleteSale" method="post")
                            input(type="hidden" name="id" value=sale._id)
                            button.btn.btn-sm.btn-outline-danger.d-flex.align-items-center.justify-content-center(type="submit" onclick="return confirm('Are you sure you want to delete this item?');")
                              i.bi.bi-trash.me-1
                               | Delete



        script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js' integrity='sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM' crossorigin='anonymous')





        